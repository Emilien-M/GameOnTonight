{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "ConnectionStrings": {
    "DefaultConnection": "" // To be configured via User Secrets or Environment Variables (e.g., for Docker)
  },
  "JwtSettings": {
    "Secret": "", // To be configured via User Secrets or Environment Variables (e.g., for Docker). MUST be a strong, random key in production.
    "ExpiryMinutes": 60,
    "Issuer": "GameOnTonight",
    "Audience": "GameOnTonightClients"
  },
  "AllowedHosts": "*"
}
